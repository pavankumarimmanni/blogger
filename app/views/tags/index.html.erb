<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag, tag_path(tag) %>
    </li>
  <% end %>
</ul>
<%= link_to "Create a new article",new_article_path, class: "new_article" %>
